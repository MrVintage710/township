package com.mrvintage.township.ui.nodes.unit;

import com.mrvintage.township.ui.nodes.Node;

import java.util.LinkedHashSet;

/// Solver for deciding how many pixel something should be based on contraints.
public class Solver {

//    private final Node root;
//    private final LinkedHashSet<Node> visited = new LinkedHashSet<>();
//
//    public Solver( Node node ) {
//        this.root = node;
//    }
//
//    public Node getParent() {
//        if (no)
//    }
//
//    private Node current() {
//
//    }
//
//    private int solve_r(Unit unit, boolean horizontal) {
//        if (unit instanceof ContextualUnit<?>) {
//            var context = ((ContextualUnit<?>) unit).getContext(this.visited.getLast(), horizontal);
//
//
//        }
//    }
//
//    private boolean shouldVisit(Node node) {
//        return visited.contains(node) || node == root;
//    }
}
